// Realizando Split (Situações Específicas)

module.exports = {
    infoCommaSplitter(info) {
       return info.split(",")
    },

    infoDashSplitter(info) {
        return info.split("-")
     }
}